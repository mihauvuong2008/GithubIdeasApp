class volumeData{
  constructor(volumeData, discount, quantily, voucherID){
    this.id =  volumeData.id;
    this.volumeData = volumeData;
    this.discount = discount;
    this.quantily = quantily;
    this.voucherID = voucherID;
  }
}
